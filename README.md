# VBase

VBase aims to be an embedded database that supports different kinds of storage models, e.g. Tree, Vector, Full Text.

The development starts with the Tree storage model, which is an LSM-Tree with levels of B-Trees.